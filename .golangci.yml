version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters:
  enable:
  - errcheck
  - govet
  - ineffassign
  - staticcheck
  - unused
  - misspell
  - revive
  - unconvert
  - unparam

  settings:
    errcheck:
      check-blank: false
      check-type-assertions: false

    govet:
      disable:
      - unsafeptr

    staticcheck:
      checks:
      - all
      - -SA1029
      - -ST1000

    revive:
      rules:
      - name: package-comments
        disabled: true
      - name: exported
        disabled: true

  exclusions:
    presets: []
    rules:
    - path: node_modules
      linters:
      - govet
    - path: _test\.go
      linters:
      - errcheck
      - staticcheck
    # cgo generates these identifiers from the C preamble. They cannot follow
    # Go naming conventions and are not user-authored Go API names.
    - path: clib/main\.go
      text: "should not use underscores in Go names"
      linters:
      - staticcheck

formatters:
  enable:
  - gofmt

  settings:
    gofmt:
      simplify: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
