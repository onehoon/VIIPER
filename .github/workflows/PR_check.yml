name: PR-Check

on:
    pull_request:
        branches:
            - "**"
    workflow_dispatch:

permissions:
    contents: read

jobs:
    build:
        uses: ./.github/workflows/build_base.yml
        secrets: inherit
        with:
            upload_artifacts: false
